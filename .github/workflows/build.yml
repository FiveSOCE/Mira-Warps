name: Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.10'
      - name: Build
        run: gradle clean build --stacktrace
      - name: Upload plugin jar
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: MiraWarps-0.1.6
          path: build/libs/MiraWarps-0.1.6.jar
          if-no-files-found: error
      - name: Publish v0.1.6 release if missing
        if: github.event_name != 'pull_request'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if ! gh release view v0.1.6 >/dev/null 2>&1; then
            gh release create v0.1.6 \
              build/libs/MiraWarps-0.1.6.jar \
              --target "${{ github.sha }}" \
              --title "MiraWarps v0.1.6" \
              --notes "Fixes player-facing /spawn execution by dispatching EssentialsSpawn as the player after MiraWarps warmup/cooldown checks, removing the misleading Console teleported you to spawn feedback."
          fi
